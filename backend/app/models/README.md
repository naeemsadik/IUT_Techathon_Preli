Purpose: backend aliases for shared API models.

Responsibilities:
- Keep backend imports discoverable for developers browsing the backend tree.
- Re-export shared contracts instead of duplicating schemas.

Future extension:
- Add backend-only persistence models in a separate persistence package when SQLite is introduced.
