Purpose: data access boundary for bot-facing API data.

Responsibilities:
- Define repository protocols.
- Provide the current mock implementation.

Future extension:
- Add SQL, Redis, or simulator-backed repositories that satisfy `BotRepository`.
