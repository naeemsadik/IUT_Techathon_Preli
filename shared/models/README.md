Purpose: Pydantic models that describe data exchanged between services.

Responsibilities:
- Validate response payloads.
- Provide reusable typed objects for backend services and bot clients.

Future extension:
- Add persistence-facing DTOs only when they are also part of an inter-service contract.
