# Simulator environment variables
# Copy to simulator/.env (or just export these in your shell) and edit as needed.

SIMULATOR_API_URL=http://127.0.0.1:8000
SIMULATOR_TOGGLE_PROB=0.2
SIMULATOR_HEARTBEAT_EVERY_N=10
# Optional: set an integer for deterministic runs
# SIMULATOR_SEED=42